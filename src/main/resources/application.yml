#指定注册中心地址
eureka:
  client:
    serviceUrl:
      defaultZone: http://120.79.153.77:8660/eureka/
  instance:
      instance-id: ${spring.cloud.client.ip-address}:${server.port}
      prefer-ip-address: true
server:
  port: 9001
spring:
   datasource:
      type: com.alibaba.druid.pool.DruidDataSource
      url: jdbc:mysql://120.79.153.77:3306/user?useUnicode=true&characterEncoding=utf-8
      username: root
      password: wawzj7788
#redis
   redis:
       host: 120.79.153.77
       port: 6379
       jedis:
         pool:
           max-idle: 8
           min-idle: 0
           max-active: 8
           max-wait: -1
       timeout: 0
         #服务的名称
   application:
     name: user-service
#mybatis 下划线转驼峰 映射配置  有了这个不用写xml
mybatis :
  configuration:
    map-underscore-to-camel-case: true
#打印sql 方便调试
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl